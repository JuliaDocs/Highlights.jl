language: julia

os:
  - linux
  - osx

julia:
  - 0.4
  - 0.5
  - nightly

notifications:
  email: false

after_success:
  - julia -e 'include(Pkg.dir("Documenter", "docs", "travis.jl"))'
  - julia -e 'include(Pkg.dir("Documenter", "test", "coverage.jl"))'
