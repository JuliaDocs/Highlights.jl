# Comments start with the `#` character.

x <- 1 + 2

hist(thing, col="#ff3300", border="pink", breaks=5)

round(runif(3, min=.1, max=2.0))

c(3, 4, 1, 3)
length(c(3L, 4L, 1L))

-Inf < (1e9 + 61.4e-12 * 1.2e2) == TRUE

a.long.variable.name == FALSE

4 %% 3

A %*% t(A)

arr[foo$bar == "X",]

c('foo', 'bar', 'baz', '"')
"...' \" ..."

while (num <= 3) {
    # ...
    if (foo == T) {
        # ...
    } else {
        # ...
    }
}

func <- function(x, y, z) {
    # ...
    return(something)
}

for (x in 1:10) {
    print(x)
}
